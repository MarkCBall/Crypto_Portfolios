<!DOCTYPE html>
<html>
  <body>
        <a href=".."><h3>Back to previous page</h3></a>
    <h1>List of portfolios for user <%= path1 %> below</h1>

    <!-- LIST OF ALL PORTOLIOS WITH DELETE BUTTON -->
    <% portfolios.forEach(function(portName) { %>
      <div class="Row">
      <a href="/<%- path1 %>/<%- portName %>"<h2><%- portName %></h2></a>
      <form method="post" action="/deletePortfolio" onsubmit="return confirm('Are you sure you want to delete this portfolio?');">
        
        <input type="hidden" name="userName" value="<%= path1 %>">
        <input type="hidden" name="portfolioName" value="<%= portName %>">
        <div class="Lbtn"><input type="submit" value="Delete Portfolio"></div>
        </form>
      </div>
     <% }) %>

     <br><br><br>
     <!-- ADD PORTFOLIO BUTTON -->
     <form method="post" action="/addPortfolio">
       <input type="text" name="portfolioToAdd" value="New_Portfolio_Name" pattern="[0-9A-Za-z_]*">
       <!-- make it so quotes, spaces ect can't be input -->
       <input type="hidden" name="userName" value="<%= path1 %>">
       <input type="submit" value="Add Portfolio">
     </form>

  </body>
</html>

